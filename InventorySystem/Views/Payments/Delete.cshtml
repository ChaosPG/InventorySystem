@model InventorySystem.Models.Payment

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_TextboxLayout.cshtml";
}

<h3>ต้องการลบข้อมูลนี้หรือไม่?</h3>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Payment_Type)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Payment_Type)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Other_Details)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Other_Details)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Bill_Number" />
        <input type="submit" value="ลบข้อมูล" class="btn btn-danger" />
        <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Index", "Payments")'">
            กลับสู่หน้าหลัก
        </button>
    </form>
</div>
